# Copy to backend.hcl (gitignored) and fill in your account id, then:
#   terraform init -backend-config=backend.hcl
bucket       = "reglens-tfstate-<ACCOUNT_ID>"
key          = "demo/terraform.tfstate"
region       = "ap-south-1"
use_lockfile = true
